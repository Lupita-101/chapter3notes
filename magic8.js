let answers=[`yes`, `no`, `maybe``Don't count on it`, `be so fr``oh no!!ask again`];

let randomNum=Math.floor(x:Math.random()*answers.length);
console.log(randomNum);
let answer = answers[randomNum]
console.log(answer);

